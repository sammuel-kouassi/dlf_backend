name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.4.5'
      SERVERPOD_PASSWORD_database: '5WkplxHkR8uGxwmGbiZY2oyEMJ2uW6jR'
      SERVERPOD_PASSWORD_redis: 'cDesIDIC0Deymvy5bwSYBicRxww-EJYO'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: dlf_backend_server
        run: docker compose up --quiet-pull --quiet-build --build -d

      - name: Install dependencies
        working-directory: dlf_backend_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: dlf_backend_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: dlf_backend_server
        run: serverpod generate

      - name: Run tests
        working-directory: dlf_backend_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: dlf_backend_server
        run: docker compose down -v
